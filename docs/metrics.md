# Model Evaluation

Generated by `python -m ml.train`. Model version `v1`, seed 42.

## Which set each number comes from

This report separates the set that *chose* things from the set that *measures* them, because mixing the two is the most common way a fraud model ends up looking better on a slide than in production.

| Set | Rows | Used for |
|---|---|---|
| Train | 354,324 | fitting the booster and the feature spec |
| Validation | 118,108 | early stopping, threshold selection |
| Test | 118,108 | **every number below, measured once** |

The split is chronological, at `TransactionDT = 8,745,782` and `12,192,854`. A random split lets card and address history bleed across the boundary and inflates everything.

Validation PR-AUC was **0.5760** and test PR-AUC is **0.4746**. The validation figure appears here once, for comparison only. It is the number early stopping optimised against, so it is not a performance claim.

Expect this to sit below what a two-way split reports for the same model family, for two reasons worth keeping separate. One is bias this design removes: with two sets, early stopping and threshold selection both run against the rows that are then reported, and the reported figure inherits that. The other is a real cost: holding back a third set leaves 354,324 training rows instead of 472,432, and moves the end of the training window further from the test window, so the model is both smaller and staler. The first is bias worth deleting. The second is the price of deleting it.

## Ranking quality (test)

| Metric | Value |
|---|---|
| **PR-AUC** | **0.4746** |
| ROC-AUC | 0.8681 |
| Baseline PR-AUC (random) | 0.0344 |
| Lift over baseline | 13.8x |
| Best iteration | 490 of 2000 |
| Features | 273 |

PR-AUC leads because the test set is 3.44% fraud: predicting 'never fraud' scores 96.6% accuracy while catching nothing, so accuracy is not reported.

## Cost model

A threshold is a business decision, and precision and recall are the wrong units for arguing about one. These three inputs convert the confusion matrix into rupees. They are **assumptions, set in `.env`, not measurements**:

| Input | Value | Meaning |
|---|---|---|
| `CHARGEBACK_FEE` | 750 | admin cost of a chargeback, on top of the lost amount |
| `REVIEW_COST` | 40 | analyst cost to clear one flagged transaction |
| `BLOCK_FRICTION_RATE` | 0.25 | share of a wrongly BLOCKed legitimate amount that walks away |

Cost of a policy = missed fraud (amount + fee) + analyst time on everything flagged + friction on legitimate transactions that reached BLOCK. Fraud that reaches REVIEW or BLOCK counts as stopped, which isolates the cost of ranking from the cost of the manual process this project does not own.

Doing nothing costs **3,657,934** over the test window: 4,064 frauds worth 609,934 plus fees.

| Policy | Review | Block | Missed fraud | Analyst time | Block friction | Net cost | Saved vs doing nothing |
|---|---|---|---|---|---|---|---|
| Configured (`.env`) | 0.30 | 0.70 | 1,316,285 | 500,360 | 45,419 | **1,862,064** | 1,795,870 (49.1%) |
| Cost-optimal on validation | 0.25 | 0.95 | 1,166,133 | 631,360 | 2,432 | **1,799,925** | 1,858,010 (50.8%) |

The cost-optimal row was chosen by sweeping 19x19 threshold pairs **on validation**, then scored once on test. Choosing thresholds on the set you report from is the same mistake as tuning on test, one level down.

Moving `RISK_REVIEW_THRESHOLD` to 0.25 and `RISK_BLOCK_THRESHOLD` to 0.95 would save a further **62,139** over this window, at the cost of +3,275 transactions in the review queue. Thresholds are policy and live in `.env`; nothing needs retraining to change them.

## At the configured REVIEW threshold (0.3), on test

| Metric | Value |
|---|---|
| Precision | 0.2094 |
| Recall | 0.6447 |
| F1 | 0.3162 |
| True positives | 2,620 |
| False positives | 9,889 |
| False negatives | 1,444 |
| True negatives | 104,155 |
| False-positive rate | 8.67% |
| False-negative rate | 35.53% |
| Legit value flagged | 1,666,002 |
| Fraud value caught | 376,649 |
| Fraud value missed | 233,285 |

## At the configured BLOCK threshold (0.7), on test

| Metric | Value |
|---|---|
| Precision | 0.5799 |
| Recall | 0.4035 |
| F1 | 0.4759 |
| True positives | 1,640 |
| False positives | 1,188 |
| False negatives | 2,424 |
| True negatives | 112,856 |
| False-positive rate | 1.04% |
| False-negative rate | 59.65% |
| Legit value flagged | 181,676 |
| Fraud value caught | 172,392 |
| Fraud value missed | 437,543 |

## Financial exposure (test)

- Total fraud value in the test window: **609,934**
- Caught at REVIEW: **376,649** (61.8%)
- Missed at REVIEW: **233,285**
- Legitimate value sent for review: **1,666,002** across 9,889 transactions

Every false positive is a real customer inconvenienced, so the legitimate value flagged is a cost, not a rounding error. That is exactly what the cost model above prices.

## Top 20 features by gain

| Feature | Gain |
|---|---|
| V258 | 5,302 |
| V201 | 1,704 |
| V69 | 1,241 |
| V90 | 1,164 |
| V283 | 612 |
| V141 | 542 |
| V187 | 497 |
| C8 | 380 |
| C14 | 333 |
| C11 | 296 |
| V312 | 290 |
| card6_freq | 268 |
| C5 | 234 |
| V336 | 232 |
| V162 | 224 |
| id_32 | 222 |
| V29 | 192 |
| ProductCD_freq | 189 |
| C1 | 188 |
| D3 | 152 |
